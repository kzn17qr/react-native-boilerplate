// @flow

export type TypeButtonWithBadge = {
  iconName: string,
  btnText?: string,
  active?: boolean,
  badgeCnt: number,
};

export type TypeBadges = {
  badgeCnt: number,
};

export type TypeInput = {
  laps: number,
  round: number,
};
